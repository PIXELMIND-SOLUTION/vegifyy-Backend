// const express = require("express");
// const router = express.Router();
// const couponController = require("../controllers/couponController");
// const authMiddleware = require("../utils/verifyToken"); // your JWT middleware

// // Create coupon (Admin only)
// router.post("/coupon", authMiddleware, couponController.createCoupon);

// // Apply coupon (User)
// router.post("/applycoupon", authMiddleware, couponController.applyCoupon);

// // Get all coupons
// router.get("/coupon", couponController.getCoupons);

// // Delete coupon (Admin only)
// router.delete("/coupon/:id", authMiddleware, couponController.deleteCoupon);

// module.exports = router;
